{
  "name": "aurix/php-auth",
  "description": "Session-based Google OAuth and email/password auth for shared PHP hosting (no Node.js required)",
  "type": "project",
  "require": {
    "php": ">=8.1",
    "ext-curl": "*",
    "vlucas/phpdotenv": "^5.6"
  },
  "autoload": {
    "psr-4": {
      "Aurix\\Auth\\": "auth-lib/src/"
    }
  },
  "config": {
    "sort-packages": true,
    "vendor-dir": "auth-lib/vendor"
  }
}
